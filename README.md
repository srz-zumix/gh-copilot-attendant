# gh-copilot-attendant

`gh-runner-kit` をベースに、基本コマンド (`completion` / `skills`) のみを含む最小構成でセットアップした GitHub CLI extension です。

## Commands

- `gh copilot-attendant completion`
- `gh copilot-attendant skills`

## Development

```bash
go test ./...
go run . --help
```
